# AI System Implementation

## Completed Tasks

- [x] **immediate-fix**: Fix the actual ZeroDv kernel account creation to properly handle smart account deployment
- [x] **test-fix**: Test the fixed implementation to ensure AA20 error is resolved
- [x] **final-fix**: Bypass ZeroDv's broken gas estimation with manual UserOp construction
- [x] **gas-limits-fix**: Increase gas limits to fix AA13 Out of Gas error during smart account deployment

## Current Work

- [ ] **viem-field-names-fix**: Fix Viem validation error by using correct field names (initCode, paymasterAndData) instead of (factory, factoryData, paymaster, paymasterData)

## Next Steps

- Test the corrected Viem field names to ensure UserOperation validation passes
- Verify smart account deployment succeeds with proper gas limits and field names
- Monitor for any remaining errors in the transaction execution flow

## Technical Details

**Progress Made:**

- ✅ AA20 "account not deployed" → Fixed (factory data added)
- ✅ AA13 "initCode failed or OOG" → Fixed (gas limits increased)
- 🔧 Viem validation error → In progress (field names corrected)

**Current Fix:**

- Changed `factory` + `factoryData` → `initCode` (Viem standard)
- Changed `paymaster` + `paymasterData` → `paymasterAndData` (Viem standard)
- Maintained higher gas limits: callGasLimit: 200k, verificationGasLimit: 200k
